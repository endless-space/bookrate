spring:
  application:
    name: bookrate-service-user
    
server:
  port: 8053

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8050/eureka/

